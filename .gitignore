# Build output
build/
build_*/
*.o
*.d
*.elf
*.bin
*.hex
*.map
*.lst

# KiCad: per-user view state, locks, caches, and auto-backups
*.kicad_prl
*.lck
~*
fp-info-cache
*-backups/
*.kicad_pcb-bak
.svg2shenzhen-cache/

# OS / editor
.DS_Store
Thumbs.db

# Third-party 3D models. The PCBs reference these as ${KIPRJMOD}/../_libs/...,
# but they are other people's files and are not redistributed here — see
# hardware/README.md for what to put in hardware/_libs/ and where to get it.
hardware/_libs/
